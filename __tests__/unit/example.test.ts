describe("Sanity check - Unit Test", () => {
  it("should correctly add two numbers", () => {
    expect(1 + 1).toBe(2);
  });

  it("should correctly check string equality", () => {
    expect("nextjs-app").toBe("nextjs-app");
  });
});
